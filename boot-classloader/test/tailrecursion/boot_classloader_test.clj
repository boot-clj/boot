(ns tailrecursion.boot-classloader-test
  (:require [clojure.test :refer :all]
            [tailrecursion.boot-classloader :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
